# gena — Python 依赖
#
# Python >= 3.9（用到 Path.is_relative_to）。已在 3.12 验证。
# 安装：
#   pip install -r requirements.txt              # 必需
#   pip install -r requirements.txt -r requirements-dev.txt   # 含测试与可选增强

# === 必需依赖 ===
requests>=2.28
python-dotenv>=1.0
# SSH 远程主机支持(utils/ssh_client.py 用)
paramiko>=3.4
